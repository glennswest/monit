[package]
name = "monit"
version = "0.3.0"
edition = "2021"
description = "Framebuffer system dashboard (local host + remote GPU host over SSH)"

[dependencies]
libc = "0.2"

[profile.release]
opt-level = 2
lto = true
strip = true
panic = "abort"
