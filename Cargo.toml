[package]
name = "monit"
version = "0.2.1"
edition = "2021"
description = "Framebuffer memory dashboard for the g8 cluster (pve + ai GPU host)"

[dependencies]
libc = "0.2"

[profile.release]
opt-level = 2
lto = true
strip = true
panic = "abort"
